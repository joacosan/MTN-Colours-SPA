export interface Icons{
    icon:string;
}

/* const b =1;
const i = b ? (console.log(b),b+3) : null

function f(g:string | number = 5,index?: number): string | number {
    return index ?  g ? index : 'pingo' : -2 || null;
}

const ie = f(3); */